#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
    string str;
    string cc[8] = {"c=", "c-", "dz=", "d-", "lj", "nj", "s=", "z="};
    cin >> str;
    
    for (int i=0; i<str.size(); i++) {
        string a = str[i] + str[i+1];
        string b = str[i] + str[i+1] + str[i+2];

        for (int j=0; j<)
    }
}